# Phase 2 — Operations MVP

## Shipped
- Launch templates (5 seeded) + create client from template API/UI
- Saved views CRUD + seed views
- Custom field defs/values on client detail
- Recurring task rules + `POST /api/v1/jobs/recurring-tick`
- AE workload API + AE dashboard status line
- `/calendar` milestone list (feature flag)
- Dashboard widgets storage model (`DashboardLayout` / `widgetsJson`)
- Automation rules (`gate.complete`, `task.created`, `risk.critical`) with hooks after mutations

## Notes
Functional MVP UI — not polished. Full Gantt deferred.
